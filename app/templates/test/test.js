const Promise = global.Promise;


describe('', function () {

    before(function () {
        // require('../server/config/db')();
    });

    after(function () {
        console.log('done');
    });

    it('should ', function (done) {

    });
});